# 스냅샷에 뜰 부품 목록 — scripts/nexar_snapshot.py --mpn-file 의 입력
#
#   python scripts/nexar_snapshot.py --mpn-file scripts/mpns.txt --out data/nexar_snapshot.json
#
# '#' 뒤는 무시되고(줄 전체·줄 끝 모두), 중복은 순서를 지킨 채 제거된다.
#
# ── 고른 기준 ────────────────────────────────────────────────────────────────
# 1. 판매자가 2곳 이상 붙을 것. 1곳뿐이면 협상 후보가 안 생겨서 이 프로젝트의
#    핵심 시연 장면(복수 후보 비교)이 성립하지 않는다. 조회가 끝나면 스크립트가
#    "판매자 1곳뿐인 품목"을 경고하니, 그 목록을 보고 다음 판에서 빼면 된다.
# 2. 개당 $0.05 이상. 초저가 수동소자(0402 저항·MLCC)는 원화 환산에서 1원 미만으로
#    뭉개져 로더가 버린다(app/snapshot_catalog.py 의 "환산 단가 0원").
# 3. 단종되지 않은 범용 부품. 리드타임·재고에 실제 값이 붙어야 스크리닝이 의미가 있다.
# 4. 이 저장소 데모의 맥락(GPU 서버랙 조립: 전원·열관리·네트워크·관리보드)에 붙는 것.
#
# ── 주의 ─────────────────────────────────────────────────────────────────────
# 무료 플랜 한도는 '조회한 파트 수'로 깎인다. 이 파일의 줄 수가 곧 비용이다.
# 반복 실행하지 말고 한 번 돌린 뒤 결과(data/nexar_snapshot.json)를 커밋할 것.

# ── 전원: 레귤레이터 / 컨버터 ────────────────────────────────────────────────
LM2596S-5.0/NOPB          # TI 벅 레귤레이터, 유통 폭이 가장 넓은 축
TPS54560DDAR              # TI 60V 벅, 산업용 전원단
NCP1117ST50T3G            # onsemi LDO
TPS62130RGTR              # TI 3A 스텝다운

# ── 전원: 스위칭 소자 / 핫스왑 ───────────────────────────────────────────────
IRFZ44NPBF                # Infineon(IR) N채널 MOSFET, 사실상 표준 부품
IRLZ44NPBF                # 로직레벨 게이트 버전
CSD18540Q5B               # TI NexFET
IPP041N12N3G              # Infineon OptiMOS
LM5069MM-2/NOPB           # TI 핫스왑 컨트롤러 — 랙 전원 삽입에 직결되는 부품
UCC21520DWR               # TI 절연 게이트 드라이버

# ── 전력 모니터링 / 절연 ─────────────────────────────────────────────────────
INA219AIDR                # TI 전류·전력 모니터 (I2C)
INA226AIDGSR              # TI 고정밀 버전
ADUM1201ARZ               # ADI 디지털 아이솔레이터

# ── 열관리 ───────────────────────────────────────────────────────────────────
LM75BD,118                # NXP 온도센서
TMP100NA/3K               # TI 온도센서
EMC2301-1-ACZL-TR         # Microchip 팬 컨트롤러

# ── 네트워크 ─────────────────────────────────────────────────────────────────
DP83867IRPAPR             # TI 기가비트 이더넷 PHY
KSZ9031RNXCA              # Microchip 기가비트 PHY

# ── 관리보드 MCU / 로직 ──────────────────────────────────────────────────────
STM32F103C8T6             # ST Cortex-M3, 유통량이 매우 많다
ATMEGA328P-AU             # Microchip AVR
SN74LVC1G14DBVR           # TI 슈미트 인버터
74HC595D,118              # Nexperia 시프트 레지스터

# ── 아날로그 ─────────────────────────────────────────────────────────────────
LM358DR                   # TI 듀얼 op-amp
NE555DR                   # TI 타이머

# ── 수동소자: 1원 미만으로 뭉개지지 않는 것만 ────────────────────────────────
IHLP2525CZER100M01        # Vishay 파워 인덕터 10uH
EEU-FR1V101               # Panasonic 전해 커패시터 100uF/35V

# ── 커넥터 ───────────────────────────────────────────────────────────────────
# 커넥터는 MPN 표기가 제조사마다 제각각이라 매칭 실패 가능성이 가장 높다.
# 결과가 비면 여기부터 의심할 것.
39-01-2040                # Molex Mini-Fit Jr 하우징 4핀 — 서버 전원 배선
5747840-4                 # TE D-sub

# ── 보류: 위 목록이 얇게 나오면 다음 판에 넣을 후보 ──────────────────────────
# 지금 넣지 않는 이유는 한도가 줄 수만큼 깎이기 때문이다. 주석을 풀면 바로 쓰인다.
# ASEMB-25.000MHZ-LC-T    # Abracon 오실레이터 (수정진동자는 MPN 형식이 까다롭다)
# 10018783-10201TLF       # Amphenol PCIe x16 슬롯
# TPS2413RSAR             # TI ORing 컨트롤러
# MAX6675ISA+T            # Maxim 열전대 인터페이스
