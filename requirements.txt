# === 런타임 핵심 ===
pydantic>=2.6           # 단계 입출력 DTO 검증·직렬화
pyyaml>=6.0             # config/categories/*.yaml 로드
python-dotenv>=1.0.0    # .env 주입

# === DB ===
boto3>=1.34,<2          # Bedrock Titan text embeddings
psycopg[binary,pool]>=3.1  # PostgreSQL + pgvector + 커넥션 풀 (src/db, db/migrate.py)

# === 백엔드 API ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# === 개발/테스트 ===
pytest>=8.0.0

# === 나중에 (실제 구현 시) ===
# 관리형 LLM API SDK      : LLM_PROVIDER 확정 후
# httpx                   : 제휴 커머스 API 호출
