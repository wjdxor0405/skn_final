# 리뷰 관측 문장 — 유저용 쉬운 문장 초안 (팀 승인 요청)

**대상 파일:** [src/repo/review_repo.py](../src/repo/review_repo.py) `ProductRiskStore.observations()` ·
`SuspectCountFile.sentence()` · `risk_store_note()`, [src/engine/stage5_explain.py](../src/engine/stage5_explain.py)
`_review_line()`, [src/services/recommendation_service.py](../src/services/recommendation_service.py) `_item_checks()`,
[frontend/js/pages/results.js](../frontend/js/pages/results.js) 리뷰 슬라이드.
**근거:** `src/CLAUDE.md` "문안 변경은 팀 승인 대상". 2026-09-14 회의(위키 `cmu11ebcm002o2qkaecpmrenv`)의
"제품별 근거 나열 대신 3~4줄 요약" 요구와 같은 방향. **이 문서는 초안이며 아직 코드에 반영하지 않았다.**

## 지금 유저가 보는 것

메인보드(ASUS ROG STRIX Z790-E, 리뷰 37건)의 "구매 전 확인" 카드. `_item_checks` 가 사용 가이드·검증 쟁점·
리뷰 관측을 `" · "` 로 이어 붙여 한 덩어리로 낸다:

> 리뷰 관측: 리뷰 37건 중 5건(13.5%)이 7일 안에 몰림 — 전체 상품 중앙값 5.5% / 리뷰어 26명이 다른 상품에서도
> 함께 나타남 (연결 상품 285개) — 중앙값 60명 / 903개 / 5점 비율 49% — 중앙값 67% / 리뷰 37건 중 8건(21.6%)이
> 의심 지표 2개 이상에 걸림 (데모 상품 전체 3.1%) — 95% 신뢰구간 [9.8, 38.2] · 기준선 초과 — 상품 단위
> 신호이며 개별 리뷰의 진위가 아닙니다

어려운 이유가 여섯 가지로 갈린다. 문장 하나만 고쳐서는 안 풀린다.

| 원인 | 예 |
|---|---|
| 왜 보는지가 없다 | "7일 안에 몰림"이 왜 중요한지, 5.5%와 13.5% 차이가 큰 건지 판단 근거가 없다 |
| 통계 용어 | 중앙값 · 95% 신뢰구간 · 기준선 · 대조군 · 부류 |
| 내부 용어 누출 | "관계·행동 축 관측 (리뷰 본문 아님)", "규칙 기반 집계", "산출물 미탑재 — pcparts_product_risk.json" |
| 행동 지시가 없다 | 읽고 나서 그냥 사면 되는지, 리뷰를 직접 봐야 하는지 모른다 |
| 다 보여준다 | 중앙값과 같은 값(몰림 5.6% vs 5.5%)까지 전부 나열해 신호가 묻힌다 |
| 모순 | Ryzen 5 5600은 헤드라인 "특이 없음"인데 바로 뒤 규칙 집계는 "기준선 초과" |

## 원칙 (docs/decisions/0001 과 충돌하지 않게)

1. **숫자는 코드가, 말은 템플릿이.** 값·중앙값·건수는 지금 그대로 두고 문장 껍데기만 바꾼다. 원문 문장은
   `evidence` 에 그대로 남기고 **`plain` 필드를 추가**한다 — 심사·검토용 원문과 유저용 문장을 분리.
2. **판정어를 쓰지 않는다.** 조작·가짜·의심스럽다 대신 "살펴볼 점". `_BANNED_IN_DRAFT` 에 `조작`·`가짜` 를 추가해
   코드로 막는다.
3. **중앙값을 넘은 것만 문장으로, 나머지는 접는다.** `[3-B]` 가 `REVIEW_AXIS_EXCESS`(2배)로 고른 지표를 그대로 쓴다.
4. **면책 문장은 한 번만, 쉬운 말로.** 지표마다 붙는 "상품 단위 신호이며…" 를 카드 하단 한 줄로 모은다.

## 3층 구조

```
[1층 헤드라인]  리뷰 37건 · 사기 전에 살펴볼 점 2가지
[2층 살펴볼 점] · 리뷰 5건(약 14%)이 같은 일주일에 몰려 올라왔어요.
                  비슷한 부품은 보통 리뷰의 5% 정도만 같은 일주일에 몰려요.
                · 리뷰 37건 중 8건(약 22%)은 '이 리뷰 하나만 남긴 계정 · 계정 활동이 짧음'
                  같은 특징이 둘 이상 겹쳐요. 리뷰가 37건뿐이라 이 숫자는 참고만 하세요.
[3층 자세히 ▾]  전체 지표 표(값 · 비슷한 부품 보통값) + 아마존에서 직접 보기 ↗ + 원문 문장
──
리뷰가 올라온 '모양'만 본 결과예요. 어떤 리뷰가 진짜인지는 판단하지 않아요.
```

특이 없는 경우(Ryzen 5 5600, 197건)는 1층 한 줄로 끝 — **"리뷰 197건 · 비슷한 부품들과 다른 점 없음"**. 2층 생략.

## 문장 템플릿

`{ }` 는 코드가 채우는 값. %는 정수로 반올림해 "약" 을 붙인다.

| 지표 | 층 | 현재 | 제안 |
|---|---|---|---|
| burst7 | 2 | 리뷰 37건 중 5건(13.5%)이 7일 안에 몰림 — 전체 상품 중앙값 5.5% | 리뷰 {건}건(약 {%}%)이 같은 일주일에 몰려 올라왔어요. 비슷한 부품은 보통 리뷰의 {중앙값}% 정도만 같은 일주일에 몰려요. |
| burst7 출시 첫 주 | 3 | (출시 첫 주 — 조작이 아니라 출시일 수 있어 랭킹 신호에서 뺐다) | 출시 직후 일주일이라 자연스럽게 리뷰가 몰린 것 같아요. |
| prolific_rate | 2 | 다작 30건+ 계정 27% — 중앙값 11% | 리뷰어 중 {%}%가 리뷰를 30건 넘게 쓴 계정이에요. 비슷한 부품은 보통 리뷰어 중 {중앙값}%만 30건 넘게 리뷰를 썼어요. |
| one_off_rate | 3 | 리뷰가 이 한 건뿐인 계정 37% — 중앙값 10% | 리뷰어 중 {%}%가 이 리뷰 하나만 남긴 계정이에요. 비슷한 부품은 보통 {중앙값}%예요. |
| short_span_rate | 3 | 활동 기간 30일 이하 계정 1% — 중앙값 8% | 리뷰어 중 {%}%가 활동 기간이 30일 이하인 계정이에요. 비슷한 부품은 보통 {중앙값}%예요. |
| verified_rate | 3 | 구매 확인 48% — 중앙값 95% (양방향 모두 증거 아님) | 리뷰 중 {%}%에 구매 확인 표시가 있어요. 비슷한 부품은 보통 {중앙값}%에 구매 확인 표시가 있어요. (체험단·증정 리뷰는 표시가 없을 수 있어요) |
| p5 | 3 | 5점 비율 90% — 중앙값 67% | 리뷰 중 {%}%가 5점 리뷰예요. 비슷한 부품은 보통 {중앙값}%가 5점 리뷰예요. |
| shared_reviewers / deg | 3 | 리뷰어 185명이 다른 상품에서도 함께 나타남 (연결 상품 1620개) — 중앙값 60명 / 903개 | **보류** — 아래 "먼저 결정할 것" 1 |
| 규칙 집계 | 2 | 리뷰 37건 중 8건(21.6%)이 의심 지표 2개 이상에 걸림 (데모 상품 전체 3.1%) — 95% 신뢰구간 [9.8, 38.2] · 기준선 초과 | 리뷰 {n}건 중 {k}건(약 {%}%)은 '{지표 이름 둘}' 같은 특징이 둘 이상 겹쳐요. [n<100 이면] 리뷰가 {n}건뿐이라 이 숫자는 참고만 하세요. |
| 헤드라인 · 특이 없음 | 1 | 리뷰 197건 관측 — 대조군 중앙값 대비 특이 없음 | 리뷰 {n}건 · 비슷한 부품들과 다른 점 없음 |
| 헤드라인 · 초과 | 1 | 리뷰 37건 관측 — 7일 몰림 13.5% (부류 중앙값 5.5%) — 검토 권장 | 리뷰 {n}건 · 사기 전에 살펴볼 점 {k}가지 |
| 관측 없음 · 문턱 미만 | 1 | 리뷰 관측 없음 — 리뷰 수 문턱 미만이거나 데이터 기간 밖 | 리뷰가 충분하지 않아요. (30건보다 적어요.) |
| 관측 없음 · 기간 밖 | 1 | 같은 문장 | 리뷰 데이터가 없어요. (2023년 9월 이후 출시) |
| 관측 없음 · 파일 없음/읽기 실패/범위 불일치 | 1 | 산출물 미탑재 — pcparts_product_risk.json | 리뷰 분석을 불러오지 못했어요. — 개발자 사유는 `reasoning_log` 에만 |
| 면책 | 하단 | 상품 단위 신호이며 개별 리뷰의 진위가 아닙니다 | 리뷰가 올라온 '모양'만 본 결과예요. 어떤 리뷰가 진짜인지는 판단하지 않아요. |
| 소스 라벨 | — | 관계·행동 축 관측 (리뷰 본문 아님) / 규칙 기반 집계 — 조작 판정 아님 | 화면에 내지 않는다. 면책 한 줄로 대신한다 |

지표 이름(규칙 집계의 `'{지표 이름 둘}'`)은 그 부품에서 가장 많이 걸린 지표 둘을 아래 이름으로 낸다:
burst → 같은 일주일에 몰림 · prolific → 리뷰를 30건 넘게 쓴 계정 · one_off → 이 리뷰 하나만 남긴 계정 ·
short_span → 계정 활동이 짧음 · unverified → 구매 확인 없음.

### 영어 (`lang="en"`)

데모는 영어로 진행한다(`src/engine/lang.py`, 2026-09-14). 영어 경로는 이미 코드에 있다 — `observations(lang="en")` 은
카드 문장을 건너뛰고 수치에서 다시 렌더하고, `SuspectCountFile.sentence` · `_review_line` · `_item_checks` 는
`L(lang, ko, en)` 으로 갈린다. 지금 영어 출력:

> 5 of 37 reviews (13.5%) arrived within a 7-day window — median across products 5.5% / 26 reviewers also appear on
> other products (285 connected products) — median 60 / 903 / 5-star share 49% — median 67% / 8 of 37 reviews (21.6%)
> trip 2+ suspicion indicators (all demo products 3.1%) — 95% CI [9.8, 38.2] · above the baseline

한국어와 같은 규칙(%로 통일 · 독립문 · "only" 는 초과 지표에만 · 판정어 금지 · 느낌표 금지)으로 같은 자리에 쓴다.

| 지표 | 층 | 제안 (en) |
|---|---|---|
| burst7 | 2 | {count} reviews (about {%}%) were posted within the same week. For similar parts, usually only about {median}% of reviews land in the same week. |
| burst7 출시 첫 주 | 3 | That week was right after launch, so the bunching looks natural. |
| prolific_rate | 2 | {%}% of the reviewers are accounts with more than 30 reviews. For similar parts, usually only {median}% of reviewers have written that many. |
| one_off_rate | 3 | {%}% of the reviewers are accounts with this one review only. For similar parts it is usually {median}%. |
| short_span_rate | 3 | {%}% of the reviewers are accounts active for 30 days or less. For similar parts it is usually {median}%. |
| verified_rate | 3 | {%}% of the reviews carry a verified-purchase mark. For similar parts, usually {median}% of reviews carry it. (Reviews from free-sample or gift programs may not carry the mark.) |
| p5 | 3 | {%}% of the reviews are 5-star. For similar parts, usually {median}% are 5-star. |
| shared_reviewers / deg | 3 | 보류 — 한국어와 같이 "먼저 결정할 것" 1 |
| 규칙 집계 | 2 | {k} of {n} reviews (about {%}%) share two or more traits such as '{two indicator names}'. [n<100 이면] With only {n} reviews, treat this number as a rough guide. |
| 헤드라인 · 특이 없음 | 1 | {n} reviews · nothing stands out compared with similar parts |
| 헤드라인 · 초과 | 1 | {n} reviews · {k} things worth a look before you buy |
| 관측 없음 · 문턱 미만 | 1 | Not enough reviews. (Fewer than 30.) |
| 관측 없음 · 기간 밖 | 1 | No review data. (Released after September 2023.) |
| 관측 없음 · 파일 없음/읽기 실패/범위 불일치 | 1 | Could not load the review analysis. |
| 면책 | 하단 | This only looks at the pattern of how reviews were posted. It does not judge whether any review is genuine. |

지표 이름(en): burst → posted within the same week · prolific → account with 30+ reviews · one_off → account with this one
review only · short_span → short-lived account · unverified → no verified-purchase mark.

영어 판정어: `_BANNED_IN_DRAFT` 의 영어 목록에 `fake` · `suspicious` · `manipulat` 를 추가한다(지금은 평가어만 있다).

**영어 경로에서 같이 고쳐야 하는 것 (실행으로 확인):**

- `risk_store_note()` 가 한국어 전용이라 영어 모드에서 `No review observation (리뷰 수 문턱 미만이거나 데이터 기간 밖)`
  처럼 섞여 나간다(`_review_line` · `stage5_explain`). "관측 없음 사유 3종" 을 가를 때 `L(lang, …)` 로 같이 갈아야 한다.
- `src/engine/lang.py` 독스트링 "리뷰 관측 문장은 데이터라 한국어 그대로" 는 낡았다 — 영어 분기는 이미 수치에서
  다시 렌더한다. 유저용 템플릿이 들어가면 두 언어 다 수치에서 렌더하고, 배치가 한국어로 적어 둔 카드 문장은
  3층 "원문" 에만 남는다. 독스트링을 그에 맞춘다.

## 템플릿 규칙 (위 표에서 뽑은 것 — 새 지표를 추가할 때도 지킨다)

1. 값은 **%로 통일**, 건수는 괄호로 같이 — "리뷰 5건(약 14%)". "절반·거의 다·넷 중 하나" 같은 어림 표현을 쓰지 않는다.
2. 비교문은 **독립문** — 주어·서술어를 다시 쓴다. "보통은 거의 다 있어요" ✗ → "보통은 구매 확인 표시가 거의 다 있어요" ✓
3. "**만**"은 중앙값을 넘어서 뽑힌 지표(몰림·다작)에만 쓴다. 양방향 지표(5점 비율·1건 계정)에는 쓰지 않는다 —
   "67%만" 은 "이 부품은 많다" 는 방향을 은근히 준다.
4. 짧게 줄이되 **"왜"는 지우지 않는다.** 괄호 한 절이면 된다 — 구매 확인의 "(체험단·증정…)", 기간 밖의 "(2023년 9월 이후 출시)".
5. 느낌표·"주의" 같은 경고 표지를 쓰지 않는다. 소표본 문장은 앞 문장을 **약하게** 하려고 붙이는 것인데 "주의!" 가
   부품 옆에 있으면 부품이 위험하다는 경고로 읽힌다.

## 템플릿에 생기는 분기

- **규칙 집계 소표본 문장**: 이미 있는 이항 95% CI 를 쓴다. CI 하한이 기준선 이하면 "살펴볼 점" 에 올리지 않고
  (코드가 이미 "구별되지 않음" 으로 판정) 3층에만 둔다. 올릴 때 n<100 이면 소표본 문장을 붙인다.
- **관측 없음 사유 3종**: 지금 `risk_store_note()` 는 "문턱 미만이거나 기간 밖" 을 한 문자열로 낸다 — 산출물에
  없다는 것만 안다. "30건보다 적어요" 를 사실대로 내려면 `data/parts_asin_map.csv` 로 갈라야 한다:
  `asin` 없음 + `note` → 기간 밖, `asin` 있는데 산출물에 없음 → 문턱 미만, 산출물 자체를 못 읽음 → 불러오지 못함.

## 어디서 만드는가

| 방식 | 판단 |
|---|---|
| **코드 템플릿** — `observations(..., style="plain")` 또는 `review_service` 에 렌더러 | **채택.** 지표가 8종뿐이라 분기를 다 쓸 수 있고, 테스트로 고정되며, 판정어가 새지 않는다 |
| LLM 재서술 (stage5 패턴: structured output + 금지어 + 템플릿 fallback) | 카드에는 쓰지 않는다. 실호출에서 "강력한 성능" 같은 평가어가 샌 전례가 있고 숫자 보존 검사를 더 얹어야 한다. 회의의 "3~4줄 요약" 에서 리뷰 한 줄을 언급할 때만 — 입력은 위 템플릿 문장, 출력 검사는 기존 `_BANNED_IN_DRAFT` |
| 프론트에서 변환 | 기각. 영어 분기·결과 에이전트 `explain` 과 갈라진다 |

프론트: "구매 전 확인" 을 **사용 가이드 / 호환 검증 / 리뷰** 세 구역으로 나눈다. `checks` 를
`{guide, validations[], review:{headline, points[], details[], footer}}` 로 구조화하고 `" · "` 결합을 버린다
(`docs/결과화면_에이전트_strands.md` 가 "앞 두 문장만 보이기" 를 프론트 몫으로 넘긴 항목). 슬라이드 상단
`조작 의심 제외` 타일은 합성값(0001)이라 실사용자에게는 리뷰 구역 헤드라인으로 대체한다.

## 먼저 결정할 것 (문장을 고치기 전에 데이터가 걸린다)

1. **`shared_reviewers` 절대수 비교.** 리뷰 4,189건 상품은 공유 리뷰어 3,073명 vs 중앙값 60명 — 리뷰가 많으면
   무조건 "초과" 로 읽힌다. 쉬운 말로 옮기면 오해가 더 커진다. `shared/n` 비율(Ryzen 94%)이나 `neighbor_density` 로
   바꾸든지, 3층 표에만 숫자로 두든지.
2. **헤드라인과 규칙 집계의 기준 불일치.** 헤드라인은 `EXCESS_KEYS`(burst7·prolific) 2배 기준, 규칙 집계는
   5지표 조합 + CI 하한 기준이라 "다른 점 없음 + 규칙 집계 초과" 가 같이 나온다. 1층 "살펴볼 점 {k}가지" 의 k 를
   한 기준으로 세든지(2층에 올라가는 문장 수 = k), 규칙 집계를 3층으로 내리든지.

## 검증

- 단위 테스트: 경로 6개(관측 없음 3종 / 다른 점 없음 / 초과 / 출시 첫 주 / n<100) × 언어 2개마다 (a) 숫자가
  문장에 그대로 있고 (b) 유저용 문장에 `조작·가짜·중앙값·신뢰구간·산출물` / `fake·suspicious·manipulat·median·CI·
  baseline` 이 없고 (c) 영어 문장에 한글이 없다(`risk_store_note` 혼용 재발 방지).
- 5초 테스트: 팀 밖 2~3명에게 카드만 보여주고 "이 부품 사기 전에 뭘 하겠나" 를 묻는다. "그냥 산다 / 리뷰 한번
  본다" 가 헤드라인과 맞으면 통과. 데모가 영어라 영어 카드로도 한다.

## 확인 요청 사항

1. 문장 템플릿 표 승인 여부 (또는 다른 표현 제안).
2. "먼저 결정할 것" 1·2 — 공유 리뷰어 지표를 비율로 바꿀지, 헤드라인 k 를 어느 기준으로 셀지.
3. 프론트 `checks` 구조 변경(문자열 → 객체)을 이번에 같이 할지, 문자열은 유지하고 줄바꿈만 넣을지.
